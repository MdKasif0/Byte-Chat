rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development mode: Allow read/write access to all authenticated users.
    // This is NOT secure for production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
